import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    primary: {
      main: '#4e342e',
    },
    secondary: {
      main: '#536b40',
    },
  },
})

export default theme
